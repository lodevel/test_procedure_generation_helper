# LLM Workflow Editor Requirements

# Qt Framework
PySide6>=6.5.0

# HTTP client for API calls
requests>=2.28.0

# Optional: For enhanced code editing (QScintilla)
# Note: QScintilla can be added later for enhanced syntax highlighting
# QScintilla>=2.13.0

# Optional: Word (.docx) export of procedures (File → Export → Word)
# Markdown export works without it; this only enables the .docx target.
python-docx>=1.1.0
# Authoring skills: SKILL.md frontmatter parsing
PyYAML>=6.0
